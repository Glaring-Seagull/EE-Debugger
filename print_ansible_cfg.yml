---
- name: prints out ansible.cfg for EE debugging
  hosts: all
  tasks:
    - name: print /etc/ansible.cfg
      msg: "the value of foo.txt is {{lookup('ansible.builtin.file', '/etc/ansible.cfg') }}"
